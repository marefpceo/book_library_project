import LoginNavbar from '../../Components/LoginNavbar'

const Dashboard = () => {
    return (
        <section>
            <LoginNavbar />
            Dashboard
        </section>
    )
}

export default Dashboard